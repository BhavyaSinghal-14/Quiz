# Quiz
Its a basic JavaScript quiz game that runs in the browser console, taking user input with prompt() and giving feedback with alert(). The quiz also has loop through predefined questions, compare answers using to LowerCase() and trim(), track the score, and displays the final result. 
